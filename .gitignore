# Build outputs
.next/
.vercel/

# Node / npm
node_modules/
.npm/

# Local dev DB (sqlite). Never commit buyer-derived data.
data/etihad.db
data/etihad.db-journal
data/etihad.db-wal
data/etihad.db-shm

# Per-install license - buyer stamped.
data/license.json
data/license.localhost.json

# Runtime dev scratch
dev.log
dev.pid

# v1.1.0: dev-only generator scripts archived here; not part of normal runtime.
scripts/dev-archive/

# Operator-only generator scripts - repo-bundled once, kept under gitignore after first commit.
# (Commented out so the generators stay in-tree. Toggle if you want them hidden.)
# scripts/gen-demo-assets.mjs
# scripts/gen-glb-reception.mjs

# Editor + IDE cache
.vscode/
.idea/
.DS_Store

# Env
.env
.env.local
.env.*.local

# Vercel
.vercel

# Local runtime caches
*.tsbuildinfo

# Graphify session-time AST extraction cache (rebuildable from source).
graphify-out/cache/
# graphify-out/graph.json, GRAPH_REPORT.md, graph.html, manifest.json
# remain tracked intentionally so cross-session continuity persists
# through git history.
